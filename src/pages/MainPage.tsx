import React, {useEffect} from 'react';
import {createTicket, ContactTypes} from '../services/ticketService';

export default () => {
    return (
        <div>
            <p>lol</p>
        </div>
    )
};
